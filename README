This library provides you with all the tools to use pandas with the Qt UI Framework. 
Just import the model and views and be happy.

Tested environments:
* Python 2.7, pandas >= 0.13, PyQt >= 4.7
* Python 3.5, pandas >= 0.18, PyQt >= 5.5