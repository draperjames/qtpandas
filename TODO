- Check for PySide Compat
- Add Tests for new view items (Edit dialog, custom table view)
- Review the flags of the DataFrameModel (How to handle bools)
- Rework setDelegatesFromDtype to not parse the whole table while changing only
  one column
- once a delegate is set and its underlying data type change, the delegate does
  not upgrade
- check descriptions of dtypes
- UNDO / REDO ?
- numpy.dtype('S') --> str / currently numpy.dtype('O') will be used to display text.